# Hysteria 重构计划：从 Go 到 Rust

本文档旨在为将 Hysteria 从 Go 语言重构为 Rust 提供一份详细的计划和指南。重构的主要目标是提升性能、安全性和可维护性，同时确保与现有 Go 版本在协议和配置文件上完全兼容。

## 1. 现有模块功能分析

Hysteria v2 的 Go 版本主要由三个核心模块组成：`app`、`core` 和 `extras`。下面将对每个模块的功能进行详细分析。

### 1.1. `app` 模块

`app` 模块是整个应用的入口和命令行界面（CLI）的实现。它负责解析命令行参数、读取配置文件，并根据用户的指令启动客户端或服务器。

- **主要功能**:
    - **命令行界面 (CLI)**: 使用 `cobra` 库构建，提供了 `client` 和 `server` 两个主要的子命令，以及其他辅助命令，如 `version`、`ping` 等。
    - **配置管理**: 使用 `viper` 库，支持 YAML 格式的配置文件，并可以通过环境变量进行覆盖。
    - **日志系统**: 使用 `zap` 库，支持 JSON 和 Console 两种格式的日志输出，并可以根据级别进行过滤。
    - **客户端模式**: 启动 Hysteria 客户端，提供 SOCKS5、HTTP 代理、TCP/UDP 转发、TProxy 和 TUN 等多种接入方式。
    - **服务器模式**: 启动 Hysteria 服务器，处理客户端连接，并提供丰富的认证、流量控制和路由功能。

### 1.2. `core` 模块

`core` 模块是 Hysteria 协议的核心实现，包含了客户端和服务器的底层逻辑。

- **主要功能**:
    - **客户端核心 (`core/client`)**: 实现了 Hysteria 客户端的核心逻辑，包括：
        - 与服务器建立 QUIC 连接。
        - 处理认证和协议协商。
        - 实现带宽控制和拥塞控制算法。
        - 管理和转发 TCP 和 UDP 流量。
    - **服务器核心 (`core/server`)**: 实现了 Hysteria 服务器的核心逻辑，包括：
        - 监听并接受客户端的 QUIC 连接。
        - 验证客户端身份。
        - 管理客户端会话和流量。
        - 将流量转发到目标地址。
    - **错误处理 (`core/errors`)**: 定义了项目中的自定义错误类型。
    - **内部实现 (`core/internal`)**: 包含了一些内部工具和协议实现，如拥塞控制、分片处理、PMTU 发现等。

### 1.3. `extras` 模块

`extras` 模块提供了一系列可选的增强功能，这些功能可以根据配置启用或禁用。

- **主要功能**:
    - **认证 (`extras/auth`)**: 提供了多种认证方式，包括密码认证、HTTP 认证和外部命令认证。
    - **流量混淆 (`extras/obfs`)**: 实现了 `salamander` 混淆算法，用于伪装流量，绕过审查。
    - **出站规则 (`extras/outbounds`)**: 实现了强大的 ACL（访问控制列表）功能，可以根据域名、IP、GeoIP/GeoSite 等规则进行流量路由和过滤。
    - **DNS 解析器 (`extras/outbounds`)**: 支持多种 DNS 解析方式，包括标准 DNS、DoH (DNS over HTTPS) 等。
    - **流量嗅探 (`extras/sniff`)**: 能够嗅探流量中的域名信息，用于更精确的路由判断。
    - **流量统计 (`extras/trafficlogger`)**: 可以通过 HTTP API 记录和查询流量使用情况。
    - **端口跳跃 (`extras/transport/udphop`)**: 客户端可以通过在一定范围内随机选择端口来连接服务器，增加抗封锁能力。
    - **伪装 (`extras/masq`)**: 服务器可以将 Hysteria 流量伪装成正常的 HTTP 流量，以迷惑审查系统。

## 2. 详细重构步骤

我们将采用分阶段的方式进行重构，确保每个阶段都有明确的产出和可验证的目标。建议为 Rust 版本创建一个新的 Cargo Workspace，并逐步将各个模块迁移过来。

### 阶段一：核心协议与库的实现 (`hysteria_core`)

这个阶段的目标是实现 Hysteria 协议的核心逻辑，并将其封装成一个独立的库。这个库将不包含任何特定于客户端或服务器的逻辑，只关注协议本身。

- **任务**:
    1.  **项目初始化**: 创建一个名为 `hysteria-rs` 的 Cargo Workspace，并在其中创建一个名为 `hysteria_core` 的 `lib` crate。
    2.  **QUIC 库选型**: 选择一个合适的 Rust QUIC 库：`s2n-quic`，并进行封装。
    3.  **协议实现**: 根据 `PROTOCOL.md` 文档，在 `hysteria_core` 中实现 Hysteria 的核心协议，包括认证、数据传输、拥塞控制等。
    4.  **单元测试**: 为核心协议的各个部分编写详尽的单元测试，确保其正确性和稳定性。

### 阶段二：客户端与服务器的实现 (`hysteria_cli`)

在核心协议库完成之后，我们将开始实现客户端和服务器的命令行程序。

- **任务**:
    1.  **CLI Crate 创建**: 在 `hysteria-rs` Workspace 中创建一个名为 `hysteria_cli` 的 `bin` crate。
    2.  **CLI 框架选型**: 选择一个合适的 Rust CLI 框架，例如 `clap`，用于解析命令行参数。
    3.  **客户端实现**: 在 `hysteria_cli` 中实现客户端逻辑，调用 `hysteria_core` 库来处理协议相关的功能。
    4.  **服务器实现**: 在 `hysteria_cli` 中实现服务器逻辑，同样调用 `hysteria_core` 库。
    5.  **集成测试**: 编写集成测试，模拟客户端和服务器之间的交互，确保端到端的连通性和功能正确性。

### 阶段三：附加功能的迁移 (`hysteria_extras`)

这个阶段将 Go 版本中的 `extras` 模块功能迁移到 Rust。

- **任务**:
    1.  **Extras Crate 创建**: 创建一个名为 `hysteria_extras` 的 `lib` crate。
    2.  **功能迁移**: 逐步将 `extras` 模块中的功能（如认证、混淆、ACL、DNS 等）用 Rust 重新实现，并放入 `hysteria_extras` crate 中。
    3.  **功能集成**: 将 `hysteria_extras` 中的功能集成到 `hysteria_cli` 的客户端和服务器中。

### 阶段四：兼容性测试与优化

在所有功能都迁移完成后，我们需要进行全面的兼容性测试，并对性能进行优化。

- **任务**:
    1.  **协议兼容性测试**: 确保 Rust 版本的客户端/服务器可以与 Go 版本的客户端/服务器正常通信。
    2.  **配置文件兼容性测试**: 确保 Rust 版本可以正确解析 Go 版本的配置文件。
    3.  **性能基准测试**: 对比 Rust 版本和 Go 版本的性能，找出潜在的瓶颈并进行优化。
    4.  **代码审计与安全加固**: 对 Rust 代码进行安全审计，修复潜在的漏洞。

## 3. 协议兼容性

保持与 Go 版本的协议兼容性是本次重构的重中之重。这意味着 Rust 版本的客户端必须能够与 Go 版本的服务器正常通信，反之亦然。

- **兼容性要点**:
    - **QUIC 握手**: 确保 Rust 版本的 QUIC 实现能够正确处理 Go 版本 `quic-go` 库的握手过程。
    - **认证流程**: Rust 版本的认证逻辑必须与 Go 版本完全一致，能够正确处理相同的认证信息。
    - **数据帧格式**: 数据传输中使用的自定义帧格式必须在两个版本之间完全一致。
    - **错误码**: 两个版本应该能够相互理解对方发送的错误码。

- **测试策略**:
    - 编写详细的协议兼容性测试套件，覆盖所有协议交互场景。
    - 在持续集成（CI）中加入兼容性测试，确保任何代码变更都不会破坏协议的兼容性。

## 4. 配置文件通用性

为了方便用户迁移，Rust 版本必须能够直接使用 Go 版本的 YAML 配置文件。

- **兼容性要点**:
    - **字段映射**: Rust 版本中的配置结构体需要与 Go 版本中的 `mapstructure` 标签完全对应。
    - **数据类型**: 确保相同配置项在两个版本中具有相同的数据类型（例如，`string`, `int`, `bool`）。
    - **默认值**: 两个版本的默认配置值应该保持一致。

- **实现策略**:
    - 使用 `serde` 和 `serde_yaml` 库来解析 YAML 配置文件。
    - 为 Rust 的配置结构体添加 `#[serde(rename = "...")]` 属性，以匹配 Go 版本中的字段名。
    - 编写单元测试，使用 Go 版本的示例配置文件来验证 Rust 版本的解析逻辑是否正确。